fastapi>=0.111
jinja2>=3.1
openai>=1.0
openpyxl>=3.1
pydantic>=2.7
python-docx>=1.1
python-multipart>=0.0.9
requests>=2.32
uvicorn>=0.30

# Required for live PDF extraction. Tests mock this adapter so installation can
# be validated before the heavier Docling dependency is added.
docling>=2.0
